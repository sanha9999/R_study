---
title: "step1_md"
output: html_document
---



### R에서의 변수 형태

Type | 설명
-|-
chr|문자열 형태
int|숫자
num|숫자
Factor|명목형 변수
Posixct|시간변수(년/월/일/시/분/초)
Tseries|시계열 변수
-----------------------

#### 명목형 변수: 특성에 따라 명칭을 주어 구분을 지어주는 변수, 변수 값에 따라 서열 정보가 존재하지 않고 모두 동등한 것으로 판단합니다.
#### Discrete(이산형): 하나, 둘, 셋, 넷 등 셀 수 있는 변수
#### 서열형 변수: 명목형 변수에서 서열 정보가 주입된 변수, 그에 따라 명목형 변수에 비해 정보량을 더 많이 포함하고 있습니다.
#### Continuous(연속형): 셀 수 없고 구간으로 정의된 변수 정보량을 가장 많이 품고 있는 변수입니다.

----------------------

### 시간(날짜) 형태의 변수 다루기
> 크게 3가지 방법이 있다.
* as.Date()를 활용하여 '년-월-일'형태로 다루기
* as.POSIXct()를 활용하여 '년-월-일 시:분:초'형태로 다루기
* lubridate 패키지를 활용하여 날짜 데이터 다루기

----------------------

### R 패키기 설치하기
> R은 프로그램이 가벼운 대신, 필요한 함수는 패키지를 설치하여 사용해야 된다.. 패키지를 설치한 후에는 항상 패키지를 R에 부착시켜야 한다.

----------------------

### 약간의 꿀팁
> 파이썬에서는 format쓰거나 어찌어찌하면 출력이 잘되는데 우리 R은 그렇지 않다.
> paste() 함수와 print()를 같이 쓰면 우리가 원하는 출력이 잘 나온다.

#### 예시
```{r}
for (i in c(2:9)) {
  for(j in c(1:9)){
    z = i * j
    print(paste(i, " * ", j, " = ", z))
  }
}
```